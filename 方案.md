# pu~ 技术方案

> 右键任何视频 → `pu~` → 手机 / iPad 扫码就能看。
> 对面不用装任何 App，浏览器打开即播。

---

## 一、这是什么

**噗**（pū）—— 拟声词。噗一下，片子就飘到另一块屏幕上了。

这个工具解决的问题很窄，但很常见：**你有一个视频文件，想在手边的另一块屏幕上看它。**

现有的做法都有摩擦：

| 做法 | 摩擦 |
|---|---|
| iTunes 文件共享 | 要连线、要装 iTunes、要在 GUI 里点四五下、目标设备必须先装播放器 App |
| 传到网盘再下载 | 上行带宽是瓶颈，2.6 GB 传两遍 |
| DLNA / 投屏 | 格式挑剔，字幕基本别想 |
| Plex / Jellyfin | 为「媒体库」设计的，为了看一个文件要先建库、刮削、装客户端 |

pu~ 的定位不是媒体服务器，是**一次性摆渡**。没有库，没有账号，没有客户端。右键一下，出一个二维码，扫完就看。看完关掉。

---

## 二、环境（已实测确认）

| 项 | 状态 |
|---|---|
| OS | Windows 11 Home China 10.0.26200 |
| .NET SDK | 9.0.302 |
| Windows SDK | 10.0.26100.0 |
| MSVC 工具集 | VS 2022 Community，**NativeAOT 可用** |
| ffmpeg / ffprobe | `D:\Application\ffmpeg\bin`（在 PATH 中） |
| git | 已安装 |
| 无 | ImageMagick / Inkscape / Python（图标链路已绕开，用 Chrome 光栅化） |

---

## 三、技术栈定型

| 项 | 选择 | 为什么不是别的 |
|---|---|---|
| 语言 | C# / .NET 9 | SDK 已就绪；Shell 集成是 Windows 一等公民场景 |
| 编译 | **NativeAOT**，单 exe ≈ 5–8 MB | 右键点一下要立刻有反应，JIT 预热在这个场景能被感知到。冷启动 < 50 ms |
| HTTP | **Kestrel** | `HttpListener` 走 http.sys，绑非 localhost 地址必须先 `netsh http add urlacl`，**需要管理员**。Kestrel 直接开 socket，普通权限即可监听 `0.0.0.0`。这是硬约束，不是偏好 |
| 界面 | 无 WinForms / WPF。托盘用 `Shell_NotifyIcon` P/Invoke，其余 UI 全是网页 | WinForms 不支持 NativeAOT。且状态页本来就要做成网页给平板看，PC 端复用同一个页面，省掉一整套 GUI 代码 |
| 转码 | 外部 ffmpeg，**不捆绑** | 捆绑要处理 GPL 分发义务。首次运行按 PATH → 配置文件 → 引导下载 的顺序探测 |

---

## 四、解决方案结构

```
pu~/
├─ Pu.sln
├─ src/
│  ├─ Pu.Core/                 引擎。不含任何 UI、不含 Windows 特有 API
│  │   ├─ Probe/               ffprobe 封装 → MediaInfo
│  │   ├─ Planning/            TranscodePlan —— 决策矩阵在这里
│  │   ├─ Pipeline/            Transcoder / SubtitleExtractor / 进度解析
│  │   ├─ Serving/             Kestrel 端点、Range 处理、会话与 token
│  │   └─ Cache/               产物索引 + LRU 淘汰
│  └─ Pu.App/                  Windows 入口
│      ├─ Program.cs           CLI 参数分发
│      ├─ Shell/               右键菜单注册 / 注销
│      ├─ Tray/                托盘图标（P/Invoke）
│      └─ Ipc/                 单实例：命名互斥体 + 命名管道
├─ web/                        播放页 / 列表页 / 状态页（编译为嵌入资源）
├─ assets/                     图标源文件与 pu.ico
├─ tools/                      构建脚本（build-icon.ps1 等）
├─ tests/Pu.Core.Tests/
└─ tmp/                        草稿与实验，已 .gitignore
```

`Pu.Core` 对 Windows 零依赖，为了两件事：**能跑单元测试**，以及以后加 `IExplorerCommand` COM 项目升级到 Win11 一级菜单时**它一行都不用改**。

---

## 五、转码决策矩阵 —— 方案的核心

大多数同类工具在这里偷懒无脑全转码。pu~ 的全部价值在于**尽可能不转**。

| 源视频 | 源音频 | 动作 | 耗时（2.6 GB 为例） |
|---|---|---|---|
| H.264 8bit + MP4 + 已 faststart | AAC | **原样服务，零处理** | 0 秒 |
| H.264 8bit（任意容器） | 任意 | 视频 copy，音频转 AAC | ≈ 2.5 分钟 |
| **HEVC 8bit** | 任意 | 视频 copy + `-tag:v hvc1`，音频转 AAC | ≈ 2.5 分钟 |
| HEVC 10bit / Hi10P / AV1 / VP9 | 任意 | 全转码 → H.264 | 慢，走硬件编码 |

### 两个必须写进代码的坑

**1. HEVC 的 codec tag**

iPad Safari 原生支持 HEVC（A9 以上 + iOS 11+），所以 H.265 片源根本不需要重编码 —— 但前提是 MP4 里的 codec tag 必须是 `hvc1`，而 ffmpeg 默认写 `hev1`。tag 不对，Safari 直接黑屏且不报错。

```
-c:v copy -tag:v hvc1
```

命中这一行，等于把最常见的「慢速全转码」场景整个消掉。

**2. faststart**

`moov` atom 默认写在文件尾部，浏览器必须下完整个文件才能起播。必须加：

```
-movflags +faststart
```

代价是 ffmpeg 结束后要再过一遍文件重写。2.6 GB 大约多花十几秒，但换来的是**秒起播**，必须做。

### 硬件加速

需要全转码时，按 `ffmpeg -encoders` 探测结果优先选 NVENC → QSV → AMF → x264 软编。硬编比软编快一个数量级，是全转码路径能否忍受的分水岭。

---

## 六、运行时流程

```
右键 pu~
  │
  ├─ 已有实例在跑？ ──是──→ 命名管道把路径递过去，复用同一个服务
  │                └─否──→ 自己成为服务端
  │
  ├─ ffprobe 探测（< 200 ms）
  ├─ 查缓存 ──命中──→ 直接出 URL
  │
  ├─ 生成 TranscodePlan，起 ffmpeg，并行抽字幕
  │
  ├─ 立刻弹出状态页（二维码 + URL）—— 此时转码还在跑
  │
  └─ 扫码打开 → 页面轮询进度 → 转完自动起播
```

**关键设计：二维码在转码开始的瞬间就给出来，不等转完。**

你扫完码，平板上先看到进度条，转好自动播。整个过程你只点了一次右键，之后手就可以离开鼠标了。

---

## 七、右键菜单集成

### v1：注册表方式（本期做）

全部写在 `HKCU` 下，**不需要管理员**：

```
HKCU\Software\Classes\SystemFileAssociations\<.ext>\shell\Pu\command   ← 约 30 个媒体扩展名
HKCU\Software\Classes\Directory\shell\Pu\command                        ← 文件夹
```

- `pu --register` / `pu --unregister`，后者干净移除
- 菜单项带 `Icon` 值指向 `pu.exe,0`
- 扩展名清单放配置文件，随时增删

**已知代价**：Windows 11 上这类菜单项落在「显示更多选项」里（或 `Shift+F10`），比一级菜单多一次点击。

### v2：Win11 一级菜单（预留）

写 `IExplorerCommand` COM 处理器 + 打稀疏 MSIX 包 + 自签名证书。证书需要管理员权限装进「受信任人」存储区。

`Pu.Core` 与 Windows 解耦就是为了这一步 —— 到时只新增一个 COM 项目，引擎不动。

### 文件夹模式

右键文件夹 → 扫描其中媒体文件 → 生成一个列表页，点进去逐个播。适合整季剧集。非媒体文件自动剔除。

---

## 八、缓存

```
%LOCALAPPDATA%\Pu\cache\{sha1(绝对路径 | 文件大小 | 修改时间)}\
```

- 同一个文件 pu 第二次是**零耗时**
- 源文件被改动（大小或 mtime 变了）→ 哈希变化 → 自动重转，不会拿到过期产物
- 默认上限 **20 GB**，LRU 淘汰
- `pu --clean` 手动清空

---

## 九、服务与安全

- 端口默认 **8000**，被占则向上探测
- URL 携带一段随机 token：`http://192.168.10.41:8000/s/{token}`

token 不是可选项。当前 Wi-Fi 被 Windows 归类为「公用网络」，同网段任何人扫到开放端口就能看到内容。加 token 后，不知道链接的人拿不到东西。

- 服务生命周期跟随托盘图标，右键托盘可「停止 pu~」
- 空闲超过 N 分钟（默认 30）自动退出，避免忘关

---

## 十、字幕

- SRT / ASS → WebVTT，全部内嵌轨都抽出来，页面上一排按钮切换
- ASS 的特效样式会丢，只保文本（WebVTT 表达能力有限，这是格式本身的边界）
- **PGS / VobSub 是图形字幕，转不了 WebVTT**。v1 直接跳过并在页面上说明原因；烧录进画面属于全转码，代价太大，不做默认行为

---

## 十一、图标

一团往右上飘的气，左下三颗渐弱的余韵表示「刚噗出来」的方向。

- 底板深色圆角方 —— 保证在浅色和深色任务栏上都立得住
- 气体薄荷绿→青，整团共用一道渐变（靠 mask 实现，不是每个圆各来一遍）
- **16 / 20 / 24 / 32 px 用简化版** `pu-small.svg`：去掉高光、余韵减到两颗、气团放大加饱和。不简化的话缩到 16px 会糊成一坨

产物 `assets/pu.ico` 含 9 个尺寸（16→256），条目为 PNG 负载。

改图标只需编辑 `assets/*.svg` 后重跑：

```powershell
.\tools\build-icon.ps1
```

脚本用 Chrome 无头模式做光栅化，不依赖 ImageMagick / Inkscape。

---

## 十二、里程碑

| | 内容 | 验收标准 |
|---|---|---|
| **M1** | Core 引擎 + CLI | `pu movie.mkv` 打通全链路，输出可用 URL |
| **M2** | 右键菜单 + 托盘 + 二维码状态页 | 完整的「右键 pu~」体验 |
| **M3** | 缓存 + 硬件加速 + 文件夹列表页 | 日常够用 |
| **M4** | NativeAOT 打包 + 安装器 | 能发给别人 |

M1 结束就已经强于现有的 Node 原型。之后每一档都能独立验收。

---

## 十三、明确不做的事

划清边界，避免摊成一个四不像的 Plex：

- ❌ 媒体库、刮削、海报墙
- ❌ 账号、多用户、权限
- ❌ 公网访问、内网穿透（只服务局域网）
- ❌ 转码农场、队列调度
- ❌ 原生移动端 App（对面只用浏览器，这是整个设计的前提）

---

## 附录：现有 Node 原型

`D:\tmp\2026-08-10-ipad-video\` 是验证可行性的一次性原型，已跑通完整链路（转封装 → Range 服务 → 12 条字幕 → iPad Safari 播放）。

它的价值是**证明了技术路线成立**，以及暴露了 faststart、Range、字幕 MIME 这几个坑。C# 版本落地后即可删除 —— 按 `CLAUDE.md` 的约定，`tmp/` 不是事实来源，正式代码不得从中导入。
